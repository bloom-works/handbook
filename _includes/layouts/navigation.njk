<nav class="nav">
  <div class="l-wrapper">
    <button class="nav-button" id="all-sections-button" data-nav-button="sections">
      <svg viewbox="0 0 16 16" fill="currentColor">
        <use xlink:href="/bootstrap-icons.svg#book"></use>
      </svg>
      <span>Sections</span>
      </button>
      <div class="nav-meta">
        <a href="{{ metadata.url }}" aria-label="Bloom Works Logo" class="nav-home">
          {% include "bloom-logo-graphic.svg" %}
          <strong> {{ metadata.guide }}</strong>
        </a>
      </div>
      {# Search button elements commented out because search isn't implemented yet; uncomment to implement search. Inline style is a duct-tape fix for header alignment and centering. #}
      <button class="nav-button" id="search-button" data-nav-button="search">
        {# 
			<svg viewbox="0 0 16 16" fill="currentColor">
			<use xlink:href="/bootstrap-icons.svg#search"></use>
			</svg>
			<span>Search</span> 
		#}
      </button>
  </div>
</nav>
<dialog id="all-sections-modal" class="all-sections-menu">
  <header>
    <span>Jump to a section</span>
    <form method="dialog">
      <button>
       <svg viewbox="0 0 16 16" fill="currentColor">
         <use xlink:href="/bootstrap-icons.svg#x-circle"></use>
       </svg>
       <span class="sr-only">Close</span>
      </button>
    </form>
  </header>
  <section class="dialog-body">
    <ol>
      {% for section in collections.sections %}
        <li>
          <a href="{{ section.page.url }}">{{ section.data.title }}</a>
        </li>
      {% endfor %}
    </ol>
  </section>
</dialog>
